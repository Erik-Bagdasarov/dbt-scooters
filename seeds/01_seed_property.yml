version: 2

seeds:
  - name: "scooters"
    config:
      delimiter: ","

  - name: "event_types"
    config:
      delimiter: ","

  - name: "age_groups"
    description: "Age groups to split users for analysis"
    config:
      delimiter: ","